ReportPortal Migrations Complex Service

The migration job has been deployed. Monitor its progress with:

  kubectl get jobs -n {{ .Release.Namespace }}
  kubectl logs job/{{ include "migrations-complex.fullname" . }} -n {{ .Release.Namespace }} --follow

To check job status:
  kubectl describe job {{ include "migrations-complex.fullname" . }} -n {{ .Release.Namespace }}

Note: The job will remain after completion for inspection. To clean up manually:
  kubectl delete job {{ include "migrations-complex.fullname" . }} -n {{ .Release.Namespace }}
